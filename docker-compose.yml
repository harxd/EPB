version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    environment:
      - INPUT_FOLDER=/app/input
      - OUTPUT_FOLDER=/app/output
